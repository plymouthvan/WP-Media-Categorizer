settings:
  wp_path: /Users/williampetruzzo/Sites/Petruzzo-Staging-Rebuild
  apply_taxonomy:
    mode: all  # Options: all, children_only, bottom_only
  backup:
    enabled: true
    output_path: /Users/williampetruzzo/Sites/backups_staging_rebuild/media-categorizer-$(date +%F_%H-%M-%S).sql
  output_csv_path: ./logs/media-categorizer-log-$(date +%F_%H-%M-%S).csv

mappings:
  Formals:
    match: "Formals"
    terms:
      - wedding > portraits
  Candids:
    match: "Candids"
    terms:
      - wedding > preparations
  Ceremony:
    match: "Ceremony"
    terms:
      - wedding > ceremony
  Details:
    match: "Details"
    terms:
      - wedding > details
  Party:
    match: "Party"
    terms:
      - wedding > party
  Portfolio:
    match: "Portfolio"
    terms:
      - wedding > portfolio
